#include "RenderManager.h"

/*
	使用する全てのxファイル読み込み
*/	
bool CRenderManager::AllLoadXFile()
{
/*	
	if ( E_FAIL == AllocateHierarchy[AN_WAIT].Load( m_pD3Device, "resource/xfile/tiny/wait.x" ) )
		return false;
	if ( E_FAIL == AllocateHierarchy[AN_RUN].Load( m_pD3Device, "resource/xfile/tiny/run.x" ) )
		return false;
	if ( E_FAIL == AllocateHierarchy[AN_JUMP].Load( m_pD3Device, "resource/xfile/tiny/jump.x" ) )
		return false;
	if ( E_FAIL == AllocateHierarchy[AN_DOWN].Load( m_pD3Device, "resource/xfile/tiny/down.x" ) )
		return false;
	if ( false == LoadXFile( MESH_BG1, "resource/xfile/stage1.x" ) )
		return false;
	if ( false == LoadXFile( MESH_BOX_1, "resource/xfile/block.X" ) )
		return false;
	if ( false == LoadXFile( MESH_BULLET, "resource/xfile/bullet.x") )
		return false;*/
	return true;

}